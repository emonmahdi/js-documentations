🧠 Closure কী?

Closure হলো এমন একটি feature যেখানে একটি inner function তার outer function এর variables মনে রাখতে পারে, even after the outer function has finished execution.

সহজ ভাষায়:
👉 Function তার parent scope এর variable “মনে রাখে”।


🎯 Basic Example:

function outer() {
  let count = 0;

  function inner() {
    count++;
    console.log(count);
  }

  return inner;
}

const counter = outer();

counter(); // 1
counter(); // 2
counter(); // 3
-----------------------------------------

🔥 Step by Step Explanation
Step 1:

outer() call হয়
→ নতুন execution context তৈরি হয়
→ count = 0

Step 2:

outer return করে inner function
কিন্তু inner এখনও count access করতে পারে।

Step 3:

outer execution শেষ হয়ে গেছে
তবুও count memory তে আছে।

👉 এটাই Closure।

-------------------------------------------
🧠 কেন Closure কাজ করে?

কারণ JavaScript lexical scoping follow করে।

Inner function যখন তৈরি হয়, তখন সে তার outer scope reference রেখে দেয়।
--------------------------------------------------

📦 Internal Concept (Deep)

Closure তৈরি হয় যখন:
Function + Lexical Environment

মানে:
Closure = Function + Reference to its Outer Scope

-------------------------------------------------
🎯 Real Life Analogy

ধরো:

তুমি একটা exam hall ছেড়ে বের হয়েছো
কিন্তু তোমার answer sheet teacher এর কাছে রয়ে গেছে।

Inner function হলো teacher
Outer variable হলো answer sheet
------------------------------------------------------
🏗 Real Project Example 1 (Private Variable)

function createUser() {
  let password = "12345";

  return {
    getPassword: function() {
      return password;
    }
  };
}

const user = createUser();

console.log(user.getPassword()); // 12345

👉 password বাইরে থেকে access করা যাচ্ছে না
এটা private হয়ে গেছে।

Closure ব্যবহার করে encapsulation করা যায়।

---------------------------------------------
🎯 Real Project Example 2 (Counter System)
function counter() {
  let count = 0;

  return function() {
    return ++count;
  };
}

const like = counter();

console.log(like()); // 1
console.log(like()); // 2

👉 React এর useState concept ও closure এর উপর ভিত্তি করে।
-------------------------------------
🧠 Closure & Memory

⚠️ Important:

Closure variable memory তে থাকবে যতক্ষণ পর্যন্ত:

Function reference আছে

যদি reference remove হয় → garbage collector clean করবে।
------------------------------------------
🧠 Deep Interview Questions

❓ 1. Why do we need closures?

Answer:

Data privacy
Encapsulation
Maintaining state
Callback functions
Functional programming

❓ 2. How does closure relate to lexical scope?

Answer:
Closure works because of lexical scoping. Functions remember the environment in which they were created.

❓ 3. Can closure cause memory leak?

Answer:
Yes, if references are not cleaned properly, it can prevent garbage collection.

❓ 4. Where is closure used in real life?

setTimeout
Event handlers
React Hooks
Module pattern
Private variables

❓ 5. Closure vs Scope difference?

Scope → accessibility rule
Closure → function retaining scope

🏆 Powerful Interview Answer (Impressive Version)

তুমি যদি impress করতে চাও, এইভাবে বলবে:

Closure is created when a function remembers its lexical environment even after the outer function has executed. It allows data encapsulation, private variables, and state preservation. Closures work because of JavaScript’s lexical scoping mechanism. It is heavily used in asynchronous programming, callbacks, and modern frameworks like React.

Closure তৈরি হয় যখন একটি function তার lexical environment মনে রাখে, এমনকি outer function execute শেষ হওয়ার পরেও।

এটি data encapsulation, private variable তৈরি করা এবং state সংরক্ষণ করার সুবিধা দেয়।

Closure কাজ করে JavaScript-এর lexical scoping mechanism এর কারণে।

Asynchronous programming, callback function এবং আধুনিক framework যেমন React-এ Closure ব্যাপকভাবে ব্যবহৃত হয়।
