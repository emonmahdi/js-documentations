const react =
  "React is a Js framework && this framework is most popular front framework right now!";

const replace = react.replace(/framework/g, "library");
// console.log(replace);

// remove white space

const myName = "My Name is Emon Mahdi. I am 26 years old.";
const removeSpace = myName.replace(/\s/g, "");
console.log(removeSpace);
