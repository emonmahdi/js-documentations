দারুন! নিচে **JavaScript এর ৩০টি সবচেয়ে বেশি জিজ্ঞাসিত সাক্ষাৎকার (Interview) প্রশ্ন**—
**সহজ সংজ্ঞা, কেন ব্যবহার হয়, রিয়েল লাইফ ব্যাখ্যা, ১টি কোড উদাহরণ, এবং ইন্টারভিউতে জিজ্ঞাসা হওয়া প্রশ্ন-উত্তর**
**বাংলায় প্রফেশনালভাবে সাজানো** দিলাম।

> ❗ এগুলো দেখে আপনি যে কোনো JS interview 80–90% confident থাকতে পারবেন।
> ➤ চাইলে শেষে আমি এগুলো **PDF**, **short notes**, অথবা **MCQ** আকারেও বানিয়ে দেবো।

---

# ✅ **1. Hoisting**

### ✔ সংজ্ঞা

Hoisting মানে হলো—জাভাস্ক্রিপ্ট কোড রান হওয়ার আগে সমস্ত **variable** এবং **function declaration** কে মেমোরিতে উপরে তুলে নেয়।

### ✔ কেন প্রয়োজন?

এতে করে আমরা কোডের আগেও কোনো function বা var variable ব্যবহার করতে পারি।

### ✔ রিয়েল লাইফ উদাহরণ

যেমন- আপনি ক্লাসে উপস্থিতি নেওয়ার আগে স্টুডেন্ট লিস্টটা রেজিস্টারে আগেই থাকে।

### ✔ কোড উদাহরণ

```js
console.log(a); // undefined (hoisted)
var a = 10;

test(); // works
function test() {
  console.log("Hoisted function");
}
```

### ✔ Interview Q&A

**Q:** Hoisting কি সব variables-এর ক্ষেত্রে কাজ করে?
**A:** Declaration hoist হয়, কিন্তু **var undefined হয়**, আর **let/const TDZ-তে থাকে (reference error).**

---

# ✅ **2. Closure**

### ✔ সংজ্ঞা

Closure হল এমন একটি ফিচার যেখানে একটি inner function বাইরের function এর variables অ্যাক্সেস করতে পারে—even after the outer function is finished.

### ✔ কেন প্রয়োজন?

Data privacy, private variable, এবং function factory বানাতে।

### ✔ রিয়েল লাইফ উদাহরণ

Store-room এর চাবি আপনি একবার পেলেই পরে দরজা বন্ধ থাকলেও ভিতরের জিনিস নিতে পারবেন।

### ✔ কোড

```js
function counter() {
  let count = 0;

  return function () {
    count++;
    return count;
  };
}

const c = counter();
console.log(c()); // 1
console.log(c()); // 2
```

### ✔ Interview Q&A

**Q:** Closure দিয়ে কী সমস্যা হতে পারে?
**A:** অযথা বেশি closure ব্যবহার করলে memory leak হতে পারে।

---

# ✅ **3. this Keyword**

### ✔ সংজ্ঞা

`this` refers to **current execution context** অর্থাৎ কে function টি কল করছে।

### ✔ কেন প্রয়োজন?

Object method, event handler, class—সব জায়গাতেই প্রয়োজন।

### ✔ রিয়েল লাইফ উদাহরণ

“আমি” শব্দটি কে ব্যবহার করছে তার উপর অর্থ বদলায়।

### ✔ কোড

```js
const user = {
  name: "Emon",
  show() {
    console.log(this.name);
  },
};
user.show(); // "Emon"
```

### ✔ Interview Q&A

**Q:** Arrow function-এ this কিভাবে কাজ করে?
**A:** Arrow function নিজস্ব this তৈরি করে না; parent-এর this ব্যবহার করে।

---

# ✅ **4. Event Loop**

### ✔ সংজ্ঞা

Event Loop JS-এর মেকানিজম যা call stack খালি হয়ে গেলে অপেক্ষমান asynchronous tasks (callback queue, microtask queue) এক্সিকিউট করে।

### ✔ কেন প্রয়োজন?

Asynchronous behavior (API, timer, promise) handle করতে।

### ✔ কোড

```js
console.log("1");
setTimeout(() => console.log("2"), 0);
console.log("3");

// Output: 1 3 2
```

### ✔ Interview Q&A

**Q:** Promise আর setTimeout কোন queue-তে যায়?
**A:** Promise → Microtask queue
setTimeout → Callback queue

---

# ✅ **5. var vs let vs const**

| Keyword | Scope    | Hoisting            | Reassign | Use Case        |
| ------- | -------- | ------------------- | -------- | --------------- |
| var     | function | hoisted (undefined) | Yes      | older JS        |
| let     | block    | TDZ                 | Yes      | modern variable |
| const   | block    | TDZ                 | No       | constants       |

### ✔ কোড

```js
let x = 10;
x = 20;
const y = 5;
// y = 6; // error
```

### ✔ Interview Q&A

**Q:** কোনটাতে TDZ হয়?
**A:** let এবং const।

---

# ✅ **6. Spread vs Rest**

### ✔ Spread (...)

একটি array/object কে ভেঙে দেয়।

### ✔ Rest (...)

একাধিক value কে একত্র করে array বানায়।

### ✔ কোড

```js
const arr = [1, 2];
const newArr = [...arr, 3]; // spread

function sum(...nums) {      // rest
  return nums.reduce((a, b) => a + b);
}
```

### ✔ Interview Q&A

**Q:** Spread এবং Rest-এর syntax একই কেন?
**A:** একই হলেও usage context আলাদা।

---

# 🔥 নিচেরগুলো একই format-এ লিখে দেবো যদি বলো

**(ব্যাপারটা খুব বড়—একই মেসেজে ৩০টা দিলে তোমার জন্য পড়া কঠিন হবে এবং chat limit cross করবে)**

---

# ❓ এখন তুমি বলো—

আমি কি **এভাবে ৩০টি বিষয়** একদম পুরো Complete Notes বানিয়ে দিবো?

**Option:**

1. Yes → সবগুলো এমন format-এ বানাও
2. Give me 10 first
3. Make a PDF
4. Make Interview Short Notes Only
5. Make MCQ Version

তুমি কোনটা চাই?


ঠিক আছে! আমি **এভাবে ৩০টি বিষয়ই — একই format (সংজ্ঞা + কেন দরকার + বাস্তব উদাহরণ + ১টি কোড + Interview Q&A)** দিয়ে পুরো Complete Professional JavaScript Interview Notes বানিয়ে দেবো।

📌 কিন্তু **একবারে ৩০টা দিলে মেসেজ লিমিটে আটকে যাবে** (ChatGPT একসাথে এত বড় মেসেজ পাঠাতে দেয় না)।
তাই আমি তোমাকে **ব্যাচ অনুযায়ী (Batch-1, Batch-2…) করে সাজানোভাবে** দেবো।

---

# ✅ **Batch–1: JavaScript Top Interview Concepts (1–5)**

Hoisting, Closure, this keyword, Event Loop, var-let-const
(আগে এগুলো দিয়েছিলাম—পরবর্তী Batch শুরু করছি **6 থেকে**।)

---

# ✅ **Batch–2: Concepts 6–10**

---

# ⭐ **6. Spread vs Rest Operator**

### ✔ সংজ্ঞা

**Spread (`...`)** → array/object কে ভেঙে আলাদা করে।
**Rest (`...`)** → একাধিক value কে একত্র করে array করে।

### ✔ কেন ব্যবহার হয়?

* Spread → copy, merge, array/object expand
* Rest → unlimited arguments handle করা

### ✔ বাস্তব উদাহরণ

Spread = ব্যাগ খুলে জিনিসপত্র পুরো বের করা
Rest = আলাদা আলাদা জিনিস এক ব্যাগে ঢোকানো

### ✔ কোড

```js
// Spread
const arr = [1, 2];
const result = [...arr, 3];

// Rest
function sum(...nums) {
  return nums.reduce((a, b) => a + b);
}
console.log(sum(1, 2, 3)); // 6
```

### ✔ Interview Q&A

**Q:** Spread এবং Rest একই দেখতে কেন?
**A:** কারণ JavaScript Context sensitive— Spread “expand” করে, Rest “collect” করে।

---

# ⭐ **7. Prototype Inheritance**

### ✔ সংজ্ঞা

JS-এ প্রতিটি object এর একটি hidden property থাকে — `[[Prototype]]` যা অন্য object-এর property inherit করতে পারে।

### ✔ কেন ব্যবহার হয়?

* Code reuse
* Memory save
* Object-oriented design

### ✔ বাস্তব উদাহরণ

আপনি আপনার বাবার property ব্যবহার করতে পারেন → inheritance।

### ✔ কোড

```js
const parent = {
  greet() {
    console.log("Hello");
  }
};

const child = Object.create(parent);
child.greet(); // inherited
```

### ✔ Interview Q&A

**Q:** Prototype chain কী?
**A:** object → prototype → prototype → null পর্যন্ত লিঙ্ককে prototype chain বলে।

---

# ⭐ **8. Arrow Function vs Regular Function**

### ✔ সংজ্ঞা

Arrow function আধুনিক ছোট syntax; regular function traditional function declaration।

### ✔ কেন ব্যবহার হয়?

Arrow → ছোট, concise, lexical this
Regular → own this, constructor, hoisting

### ✔ বাস্তব উদাহরণ

Arrow = ছোট শর্টকাট
Regular = পুরা full-road

### ✔ কোড

```js
const obj = {
  name: "Emon",
  regular() { console.log(this.name); },
  arrow: () => console.log(this.name)
};

obj.regular(); // Emon
obj.arrow();   // undefined (parent this)
```

### ✔ Interview Q&A

**Q:** Arrow function কি constructor হতে পারে?
**A:** না। কারণ এদের own `this` নেই।

---

# ⭐ **9. Promise vs Async/Await**

### ✔ সংজ্ঞা

Promise → async operation handle করে
Async/await → Promise ব্যবহার সহজ করে

### ✔ কেন ব্যবহার হয়?

Asynchronous tasks sequentially পড়তে সুবিধা

### ✔ বাস্তব উদাহরণ

Promise = চুক্তি
Async/await = চুক্তি পাওয়া চূড়ান্ত ফলাফলের অপেক্ষা

### ✔ কোড

```js
// Promise
getData().then(res => console.log(res));

// Async/await
async function fetchData() {
  const res = await getData();
  console.log(res);
}
```

### ✔ Interview Q&A

**Q:** Async/await internally কী ব্যবহার করে?
**A:** Promises।

---

# ⭐ **10. Map vs forEach**

### ✔ সংজ্ঞা

map → নতুন array return করে
forEach → কিছু return করে না

### ✔ কেন ব্যবহার হয়?

map → transform array
forEach → iteration only

### ✔ বাস্তব উদাহরণ

map = ইনপুট → আউটপুট লিস্ট
forEach = শুধু ঘুরে দেখা

### ✔ কোড

```js
const nums = [1, 2, 3];

const doubled = nums.map(n => n * 2); // [2,4,6]

nums.forEach(n => console.log(n)); // just logs
```

### ✔ Interview Q&A

**Q:** map কি mutation করে?
**A:** না, নতুন array দেয়।

---

# ✨ এখন বলো → আমি কি **Batch–3 (11 থেকে 15) শুরু করবো?**

**filter vs find, Deep vs Shallow, == vs ===, Null vs undefined, Mutable vs immutable**

বললেই শুরু করছি।

ধন্যবাদ! চলুন শুরু করি **Batch–3: 11 থেকে 15 পর্যন্ত** —  একই ফরম্যাটে।

---

# ✅ **11. filter vs find**

### ✔ সংজ্ঞা

* **filter:** Array থেকে সব matching element নিয়ে নতুন array দেয়।
* **find:** Array থেকে প্রথম matching element দেয়।

### ✔ কেন ব্যবহার হয়?

* filter → একাধিক মান পাওয়া
* find → শুধু প্রথম মান পাওয়া

### ✔ বাস্তব উদাহরণ

filter = দোকানে সব লাল জামা খুঁজে বের করা
find = দোকানে প্রথম লাল জামা পাওয়া

### ✔ কোড

```js
const arr = [1, 2, 3, 4, 5];

// filter সব even number  
const evens = arr.filter(num => num % 2 === 0); // [2,4]

// find প্রথম even number  
const firstEven = arr.find(num => num % 2 === 0); // 2
```

### ✔ Interview Q&A

**Q:** filter কি প্রথম match পেয়ে থামে?
**A:** না, পুরো array scan করে।
**Q:** find কি সব match খোঁজে?
**A:** না, প্রথম match পেয়ে থেমে যায়।

---

# ✅ **12. Deep vs Shallow Copy**

### ✔ সংজ্ঞা

* **Shallow copy:** শুধু এক লেভেল copy, nested object reference দেয়।
* **Deep copy:** nested objects সহ সম্পূর্ণ আলাদা copy তৈরি করে।

### ✔ কেন প্রয়োজন?

Mutable data থেকে unexpected change রোধ করতে।

### ✔ বাস্তব উদাহরণ

Shallow copy = কাগজের কপি (জিনিস শুধু ঠিকানা দেয়)
Deep copy = আসল বস্তু নতুন করে বানানো

### ✔ কোড

```js
const obj = { a: 1, b: { c: 2 } };

// Shallow copy
const shallow = {...obj};
shallow.b.c = 20;
console.log(obj.b.c); // 20 (changed)

// Deep copy (JSON)
const deep = JSON.parse(JSON.stringify(obj));
deep.b.c = 30;
console.log(obj.b.c); // 20 (unchanged)
```

### ✔ Interview Q&A

**Q:** Shallow copy কোন ক্ষেত্রে সমস্যা দেয়?
**A:** Nested object পরিবর্তনে মূল অবজেক্টও পরিবর্তিত হয়।

---

# ✅ **13. Null vs Undefined**

### ✔ সংজ্ঞা

* **undefined:** value দেয়নি, variable declared কিন্তু value নেই।
* **null:** explicitly value নেই (empty বা খালি মান)

### ✔ কেন প্রয়োজন?

Variable initialization, intentional empty value পার্থক্য বোঝাতে।

### ✔ বাস্তব উদাহরণ

undefined = অনুপস্থিত তথ্য (খোঁজে পেল না)
null = খালি বাক্স যা আমরা নিজে দিলাম

### ✔ কোড

```js
let a;
console.log(a); // undefined

let b = null;
console.log(b); // null
```

### ✔ Interview Q&A

**Q:** null এবং undefined কি একই?
**A:** না, typeof undefined = "undefined", typeof null = "object"।

---

# ✅ **14. == vs ===**

### ✔ সংজ্ঞা

* `==` → value তুলনা করে, type conversion দিয়ে।
* `===` → value এবং type দুইই check করে (strict equality)।

### ✔ কেন প্রয়োজন?

Type coercion এ বিভ্রান্তি এড়াতে।

### ✔ বাস্তব উদাহরণ

`==` = তুলনা করার সময় রূপান্তর করে, `===` = সঠিক মিল চায়।

### ✔ কোড

```js
console.log(5 == "5");  // true (type conversion)
console.log(5 === "5"); // false (different types)
```

### ✔ Interview Q&A

**Q:** কোনটা বেশি safe?
**A:** `===` কারণ এটি টাইপও চেক করে।

---

# ✅ **15. Mutable vs Immutable**

### ✔ সংজ্ঞা

* **Mutable:** value পরিবর্তনযোগ্য।
* **Immutable:** value পরিবর্তনযোগ্য নয়।

### ✔ কেন প্রয়োজন?

Bug কমাতে, predictable code লিখতে।

### ✔ বাস্তব উদাহরণ

Mutable = প্লাস্টিকের বোতল, পরিবর্তন করা যায়
Immutable = কাঁচের গ্লাস, ভাঙ্গলে পরিবর্তন হয় না

### ✔ কোড

```js
// Mutable - array
const arr = [1, 2];
arr.push(3);
console.log(arr); // [1,2,3]

// Immutable - string
let str = "Hi";
str[0] = "h"; 
console.log(str); // "Hi" (change হয় না)
```

### ✔ Interview Q&A

**Q:** JS-এ কোন data type immutable?
**A:** Primitive types যেমন string, number, boolean immutable।

---

✨ **Batch–3 শেষ!**

**চলুন এখন বলো, Batch–4 (16 থেকে 20) চাই?**
(High order function, Callback vs Promise, Event bubbling, Destructuring, Currying)

বললেই দিবো!
ঠিক আছে! চলুন শুরু করি **Batch–4: 16 থেকে 20 পর্যন্ত**।

---

# ✅ **16. Higher Order Functions**

### ✔ সংজ্ঞা

যে ফাংশনগুলো অন্য ফাংশনকে আর্গুমেন্ট হিসেবে নেয় বা ফাংশন রিটার্ন করে, তাদের Higher Order Function (HOF) বলে।

### ✔ কেন প্রয়োজন?

কোডকে modular, reusable, এবং declarative করতে।

### ✔ বাস্তব উদাহরণ

রান্নাঘরে সেইচেফ যিনি রেসিপি দেন আর কাজ করেন।

### ✔ কোড

```js
function greet() {
  return () => console.log("Hello");
}

function runTwice(func) {
  func();
  func();
}

const sayHi = greet();
runTwice(sayHi); // Hello Hello
```

### ✔ Interview Q&A

**Q:** Array methods (map, filter) কি HOF?
**A:** হ্যাঁ, কারণ তারা ফাংশনকে আর্গুমেন্ট নেয়।

---

# ✅ **17. Callback vs Promise**

### ✔ সংজ্ঞা

* **Callback:** ফাংশন যাকে অন্য async ফাংশনের শেষে কল করা হয়।
* **Promise:** async অপারেশন এর ফলাফল হ্যান্ডেল করার অবজেক্ট।

### ✔ কেন প্রয়োজন?

Callback → async কাজ সম্পন্ন হওয়ার পরে
Promise → async কোড লিখতে সহজ ও readable

### ✔ বাস্তব উদাহরণ

Callback = ডাক্তারকে ফোন দিয়ে রিপোর্ট জানানো
Promise = ডাক্তারের ওয়েবসাইটে রিপোর্ট চেক করা

### ✔ কোড

```js
// Callback
setTimeout(() => {
  console.log("Done!");
}, 1000);

// Promise
const p = new Promise(resolve => setTimeout(() => resolve("Done!"), 1000));
p.then(console.log);
```

### ✔ Interview Q&A

**Q:** Callback hell কি?
**A:** অনেক nested callback যা কোড অস্পষ্ট করে।

---

# ✅ **18. Event Bubbling**

### ✔ সংজ্ঞা

Event bubbling হচ্ছে DOM এ event handler গুলো parent থেকে child এর দিকে নয়, বরং child থেকে parent এর দিকে ধাপে ধাপে ওঠার প্রক্রিয়া।

### ✔ কেন প্রয়োজন?

Event delegation ও একাধিক event handler সহজে পরিচালনার জন্য।

### ✔ বাস্তব উদাহরণ

আপনি দোকানে কিছু কিনলে সে জিনিস দোকানের ম্যানেজার পর্যন্ত পৌঁছায়।

### ✔ কোড

```html
<div id="parent">
  <button id="child">Click Me</button>
</div>

<script>
  document.getElementById('parent').addEventListener('click', () => console.log('Parent clicked'));
  document.getElementById('child').addEventListener('click', () => console.log('Child clicked'));
</script>
```

**বাটন ক্লিক করলে**

```
Child clicked  
Parent clicked  
```

### ✔ Interview Q&A

**Q:** Event bubbling থামানোর জন্য কোন মেথড?
**A:** event.stopPropagation();

---

# ✅ **19. Destructuring**

### ✔ সংজ্ঞা

Array বা Object থেকে values বা properties আলাদা করে এক্সট্র্যাক্ট করার syntax।

### ✔ কেন প্রয়োজন?

Cleaner কোড, সহজ ভেরিয়েবল ডিক্লারেশন।

### ✔ বাস্তব উদাহরণ

সিলেক্টিভলি জিনিস আলাদা করে নেওয়া, যেমন দোকান থেকে শুধু কলা বা আপেল নেওয়া।

### ✔ কোড

```js
const user = { name: "Emon", age: 25 };
const { name, age } = user;
console.log(name, age); // Emon 25

const arr = [1, 2, 3];
const [first, second] = arr;
console.log(first, second); // 1 2
```

### ✔ Interview Q&A

**Q:** Destructuring এ default value কিভাবে দিব?
**A:**

```js
const { name = "Guest" } = {};
console.log(name); // Guest
```

---

# ✅ **20. Currying**

### ✔ সংজ্ঞা

একাধিক আর্গুমেন্ট নেওয়া ফাংশনকে ধাপে ধাপে এক এক করে আর্গুমেন্ট নেওয়ার ফাংশনে রূপান্তর করা।

### ✔ কেন প্রয়োজন?

Partial application, reusable function তৈরিতে সাহায্য করে।

### ✔ বাস্তব উদাহরণ

এক ধাপ এক ধাপ কাজ করে গাড়ি তৈরি করা।

### ✔ কোড

```js
function sum(a) {
  return function(b) {
    return a + b;
  };
}

console.log(sum(2)(3)); // 5
```

### ✔ Interview Q&A

**Q:** Currying এবং Partial Application এর মধ্যে পার্থক্য?
**A:** Currying ফাংশন আর্গুমেন্ট এক এক করে নেয়, Partial Application আগের কিছু আর্গুমেন্ট ফিক্স করে দেয়।

---

✨ **Batch–4 শেষ!**

**পরবর্তী Batch–5 (21 থেকে 25) চাও?**
(Debouncing, Throttling, Set vs Map, Factory Function, Memory Lifecycle)

বললেই দেবো।

চলুন শুরু করি **Batch–5: 21 থেকে 25 পর্যন্ত** —  একই ফরম্যাটে।

---

# ✅ **21. Debouncing**

### ✔ সংজ্ঞা

একটি ফাংশন অনেকবার কল হলেও, সেটি নির্দিষ্ট সময় পর একবারই execute হয় — সেটাই Debouncing।

### ✔ কেন প্রয়োজন?

অনেকবার কল হওয়া event (যেমন keypress, scroll) থেকে performance সমস্যা রোধ করতে।

### ✔ বাস্তব উদাহরণ

আপনি দ্রুত টাইপ করলেও সার্চ তখনই করবে যখন টাইপ করা শেষ।

### ✔ কোড

```js
function debounce(fn, delay) {
  let timer;
  return function(...args) {
    clearTimeout(timer);
    timer = setTimeout(() => fn.apply(this, args), delay);
  };
}

const process = debounce(() => console.log('Search!'), 500);
window.addEventListener('keyup', process);
```

### ✔ Interview Q&A

**Q:** Debounce এবং Throttle মধ্যে পার্থক্য?
**A:** Debounce অনেক কলের মধ্যে শেষটাকে execute করে; Throttle নির্দিষ্ট সময় পর একবার execute করে।

---

# ✅ **22. Throttling**

### ✔ সংজ্ঞা

একটি ফাংশন একটি সময়সীমার মধ্যে একবারের বেশি execute হয় না।

### ✔ কেন প্রয়োজন?

Scroll, resize ইভেন্টে একাধিকবার কল রোধ করতে।

### ✔ বাস্তব উদাহরণ

আপনি প্রতি ৫ সেকেন্ডে শুধু একবারই কাউকে ফোন করবেন।

### ✔ কোড

```js
function throttle(fn, limit) {
  let inThrottle;
  return function(...args) {
    if (!inThrottle) {
      fn.apply(this, args);
      inThrottle = true;
      setTimeout(() => (inThrottle = false), limit);
    }
  };
}

const log = throttle(() => console.log('Scroll event'), 2000);
window.addEventListener('scroll', log);
```

### ✔ Interview Q&A

**Q:** Debounce কি কোনো fixed interval এ রান করে?
**A:** না, Debounce waits till event stops.

---

# ✅ **23. Set vs Map**

| Feature    | Set                      | Map                               |
| ---------- | ------------------------ | --------------------------------- |
| Data Type  | Unique values collection | Key-value pairs                   |
| Order      | Insertion order          | Insertion order                   |
| Key Access | No                       | Yes                               |
| Duplicate  | No                       | Keys unique, values can duplicate |

### ✔ কেন ব্যবহার হয়?

Set → unique value list চাইলে
Map → key-value pair structured data

### ✔ কোড

```js
const set = new Set([1, 2, 2, 3]);
console.log(set); // Set(3) {1,2,3}

const map = new Map();
map.set('name', 'Emon');
console.log(map.get('name')); // Emon
```

### ✔ Interview Q&A

**Q:** Set কি index দিয়ে access করা যায়?
**A:** না, কিন্তু spread বা Array.from() দিয়ে array করা যায়।

---

# ✅ **24. Factory Function**

### ✔ সংজ্ঞা

একটি ফাংশন যা নতুন object তৈরি করে return করে।

### ✔ কেন প্রয়োজন?

Reusable object creation, avoid duplication।

### ✔ বাস্তব উদাহরণ

কল সেন্টার থেকে নতুন নতুন আইডি দেওয়া।

### ✔ কোড

```js
function createUser(name, age) {
  return {
    name,
    age,
    greet() {
      console.log(`Hi, I'm ${this.name}`);
    }
  };
}

const user1 = createUser('Emon', 25);
user1.greet(); // Hi, I'm Emon
```

### ✔ Interview Q&A

**Q:** Factory function আর Constructor function এর পার্থক্য?
**A:** Constructor function new keyword দিয়ে চালানো হয়, factory function শুধু return করে।

---

# ✅ **25. Memory Lifecycle**

### ✔ সংজ্ঞা

JavaScript এ variable এর memory allocation, use, এবং release (garbage collection) এর পুরো প্রক্রিয়া।

### ✔ কেন প্রয়োজন?

Memory leak থেকে বাঁচতে, performance বাড়াতে।

### ✔ বাস্তব উদাহরণ

আপনি ঘর পরিষ্কার করেন পুরোনো জিনিস ফেলে দিয়ে।

### ✔ কোড

```js
function foo() {
  let a = 10; // memory allocated
  return function() {
    console.log(a); // closure keeps 'a' in memory
  };
}

const bar = foo();
bar();
```

### ✔ Interview Q&A

**Q:** JS Garbage Collector কীভাবে কাজ করে?
**A:** Unreachable objects (যাদের আর reference নেই) মেমোরি থেকে মুক্ত করে।

---

✨ **Batch–5 শেষ!**

**Batch–6 (26 থেকে 30) চাই?**
(Block scope vs function scope, TDZ, Pure function, Error handling, JS single-threaded model)

বললেই দিচ্ছি।
চলুন শেষ করি **Batch–6: 26 থেকে 30 পর্যন্ত** —  একই ফরম্যাটে।

---

# ✅ **26. Block Scope vs Function Scope**

### ✔ সংজ্ঞা

* **Block Scope:** `{}` এর মধ্যে সীমাবদ্ধ ভেরিয়েবল (let, const)
* **Function Scope:** ফাংশনের মধ্যে সীমাবদ্ধ ভেরিয়েবল (var)

### ✔ কেন প্রয়োজন?

Variable leakage রোধ করে, নিরাপদ কোড।

### ✔ বাস্তব উদাহরণ

Block scope = ঘর
Function scope = বাড়ির পুরো ফ্লোর

### ✔ কোড

```js
function test() {
  if (true) {
    var a = 1;   // function scope
    let b = 2;   // block scope
  }
  console.log(a); // 1
  console.log(b); // ReferenceError
}
test();
```

### ✔ Interview Q&A

**Q:** var এবং let/const এর scope পার্থক্য কী?
**A:** var function-scoped, let/const block-scoped।

---

# ✅ **27. TDZ (Temporal Dead Zone)**

### ✔ সংজ্ঞা

`let` ও `const` ভেরিয়েবল ডিক্লেয়ার হওয়ার আগে এর access করলে error হয়—এটাকে Temporal Dead Zone বলে।

### ✔ কেন প্রয়োজন?

Variable-কে early access থেকে রক্ষা করতে।

### ✔ বাস্তব উদাহরণ

একটা বাক্স খুলার আগেই তার জিনিস খোঁজা।

### ✔ কোড

```js
console.log(a); // ReferenceError
let a = 10;
```

### ✔ Interview Q&A

**Q:** var কি TDZ এ পড়ে?
**A:** না, var hoisted এবং undefined দিয়ে ইনিশিয়াল হয়।

---

# ✅ **28. Pure Function**

### ✔ সংজ্ঞা

একটি ফাংশন যা একই ইনপুট দিলে সবসময় একই আউটপুট দেয় এবং কোনো বাইরের state পরিবর্তন করে না।

### ✔ কেন প্রয়োজন?

Predictable, testable, bug-free কোডের জন্য।

### ✔ বাস্তব উদাহরণ

Mathematical function যেমন `sum(a,b)`।

### ✔ কোড

```js
function add(a, b) {
  return a + b;
}
```

### ✔ Interview Q&A

**Q:** Pure function কি side effect করে?
**A:** না, pure function কখনো side effect করে না।

---

# ✅ **29. Error Handling**

### ✔ সংজ্ঞা

JS-এ runtime error manage করার পদ্ধতি, যেমন try-catch-finally block ব্যবহার।

### ✔ কেন প্রয়োজন?

Program crash রোধ ও graceful failure এর জন্য।

### ✔ বাস্তব উদাহরণ

গাড়ি চলাকালীন সমস্যা হলে নিরাপদে থামানো।

### ✔ কোড

```js
try {
  let result = riskyOperation();
  console.log(result);
} catch (error) {
  console.error("Error caught:", error);
} finally {
  console.log("Always runs");
}
```

### ✔ Interview Q&A

**Q:** finally block কখন execute হয়?
**A:** সব সময়, error থাকুক বা না থাকুক।

---

# ✅ **30. JS Single-threaded Model**

### ✔ সংজ্ঞা

JavaScript একসাথে শুধু একটি কাজ করতে পারে (single thread) কিন্তু asynchronous operation দিয়ে concurrency আনে।

### ✔ কেন প্রয়োজন?

Simple concurrency management এবং event-driven programming।

### ✔ বাস্তব উদাহরণ

একজন রাঁধুনি একসাথে একবার একরকম খাবার রান্না করে।

### ✔ কোড

```js
console.log("Start");

setTimeout(() => {
  console.log("Timeout");
}, 0);

console.log("End");

// Output: Start, End, Timeout
```

### ✔ Interview Q&A

**Q:** JS single-threaded হলেও async কাজ কিভাবে হয়?
**A:** Event loop ও callback queue দিয়ে।

---

🎉 **এই হলো তোমার ৩০টি JavaScript Interview Concept-এর সম্পূর্ণ প্রফেশনাল নোটস!**

---

## তোমার জন্য পরবর্তী সাজেশন:

* এগুলো নিয়ে **MCQ তৈরি করব?**
* বা **Short Summary Sheet বানাবো?**
* কিংবা **Practice Problems দিতে পারি**?

বললে কাজ শুরু করি!
