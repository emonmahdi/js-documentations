Below is **Module 8: Asynchronous JavaScript** — fully explained in Bangla with deep concepts, interview-level answers, diagrams, and code examples.

---

# ✅ **MODULE 8: Asynchronous JavaScript (A to Z)**

JavaScript is **single-threaded**, but it handles asynchronous tasks using **Event Loop**, **Web APIs**, and **Callback Queue**.

---

# 🔹 1) **Synchronous vs Asynchronous**

### ✔ Synchronous (Line-by-line execution)

এক লাইন শেষ না হলে পরের লাইন চলে না।

```js
console.log("1");
console.log("2");
console.log("3");
```

---

### ✔ Asynchronous (Non-blocking)

যে কাজ সময় নেয় (API, Timer) সেটা পরে execute হয়।

```js
console.log("1");

setTimeout(() => {
  console.log("2");
}, 2000);

console.log("3");
```

👉 Output:

```
1
3
2
```

---

# 🔹 2) **Event Loop (Most Asked in Interviews)**

Event loop ensures:

* Call Stack empty হলে
* Queue থেকে async callbacks call stack-এ পাঠায়

### ✔ Simple Explanation:

1. JS main thread → synchronous code execute করে
2. async কাজ → Web APIs handle করে
3. শেষ হলে callback queue-তে দেয়
4. Event loop queue → call stack-এ push করে

---

### 🔥 Visual Diagram (Very Important)

```
         ┌──────────────┐
         │  Call Stack   │
         └──────┬───────┘
                │
       Event Loop checks
                │
  ┌─────────────▼───────────────┐
  │      Callback Queue          │
  └─────────────┬───────────────┘
                │
  ┌─────────────▼───────────────┐
  │      Web APIs (Timers, Fetch)│
  └──────────────────────────────┘
```

---

# 🔹 3) **Callback Function**

Callback এমন function যেটা অন্য function-এ parameter হিসাবে যায় এবং পরে execute হয়।

```js
function greet(name, callback) {
  console.log("Hello " + name);
  callback();
}

greet("Emon", () => {
  console.log("Welcome!");
});
```

---

## ❌ Callback Hell (Nested Callbacks)

```js
setTimeout(() => {
  console.log("1");
  setTimeout(() => {
    console.log("2");
    setTimeout(() => {
      console.log("3");
    }, 1000);
  }, 1000);
}, 1000);
```

👉 Hard to read & maintain
👉 Solution → **Promise + async/await**

---

# 🔹 4) **Promises**

Promise তিনটি state:

1. pending
2. fulfilled
3. rejected

### ✔ Example:

```js
const promise = new Promise((resolve, reject) => {
  let success = true;

  if (success) {
    resolve("Success!");
  } else {
    reject("Failed!");
  }
});

promise
  .then(res => console.log(res))
  .catch(err => console.log(err));
```

---

# 🔹 5) **Promise Chaining**

```js
fetchUser()
  .then(user => fetchPosts(user.id))
  .then(posts => console.log(posts))
  .catch(error => console.log(error));
```

---

# 🔹 6) **async / await (Most Used in Modern JS)**

✔ async function → promise return করে
✔ await → promise resolve না হওয়া পর্যন্ত wait করে

### Example:

```js
async function getData() {
  try {
    const res = await fetch("https://jsonplaceholder.typicode.com/posts");
    const data = await res.json();
    console.log(data);
  } catch (error) {
    console.log("Error:", error);
  }
}

getData();
```

---

# 🔹 7) **try / catch**

async/await ব্যবহার করলে error ধরার সবচেয়ে clean উপায়।

```js
async function fetchUser() {
  try {
    let res = await fetch("https://api.com/user");
    let data = await res.json();
    console.log(data);
  } catch (err) {
    console.log("Error:", err);
  }
}
```

---

# 🔹 8) **Microtask Queue vs Macrotask Queue**

### ✔ Microtask Queue

* `.then()` callbacks
* `catch()`, `finally()`
* async/await resolved tasks

### ✔ Macrotask Queue

* setTimeout
* setInterval
* DOM events

👉 Microtasks always run **before** macrotasks.

---

# 📌 Example (Very Important in Interview)

```js
console.log("1");

setTimeout(() => console.log("2"), 0);

Promise.resolve().then(() => console.log("3"));

console.log("4");
```

### Output:

```
1
4
3   <- Microtask
2   <- Macrotask
```

---

# 🎯 INTERVIEW FOCUS ANSWERS

---

## ✔ **1) Event Loop explained (Interview Answer)**

**Answer:**
JavaScript single-threaded হলেও asynchronous tasks handle করতে event loop ব্যবহার করে।
Call stack empty হলে event loop microtask queue → macrotask queue থেকে callbacks নিয়ে stack-এ push করে।

Execution order:

1. Synchronous
2. Microtasks (Promises)
3. Macrotasks (Timers)

---

## ✔ **2) Promise vs Async/Await**

### Promise:

* `.then()`, `.catch()`
* chaining হয়
* callback-like structure

### async/await:

* synchronous-style দেখায়
* cleaner & easier to read
* error handling simple (try/catch)

---

## ✔ **3) Call Stack vs Callback Queue**

| Topic     | Call Stack       | Callback Queue       |
| --------- | ---------------- | -------------------- |
| কাজ       | Function execute | Async callback store |
| Filled by | JS Engine        | Web APIs             |
| Order     | LIFO             | FIFO                 |
| Example   | function, loop   | setTimeout, event    |

---

# 🧪 **Practice Tasks (Must Do)**

### ✔ Task 1

Promise দিয়ে delay function:

```js
delay(1000).then(() => console.log("1 second later"));
```

### ✔ Task 2

async/await দিয়ে user → posts fetch:

```
getUser()
getPosts(user.id)
```

### ✔ Task 3

Event loop output trace:

```js
console.log("A");

setTimeout(() => console.log("B"), 0);

Promise.resolve().then(() => console.log("C"));

console.log("D");
```

Explain output.

---

If you want, I can give you **Module 9: DOM & Browser APIs** next.
