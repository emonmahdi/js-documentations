# Condition কী?

Condition মানে হলো —
👉 কোনো সিদ্ধান্ত নেওয়া

উদাহরণ:

যদি বয়স ≥ 18 হয় → ভোট দিতে পারবে

নাহলে → পারবে না

এই সিদ্ধান্ত নেওয়ার জন্যই condition ব্যবহার করা হয়।


# if Statement
✅ Syntax
if (condition) {
  // code run হবে যদি condition true হয়
}

উদাহরণ
let age = 20;

if (age >= 18) {
  console.log("You can vote");
}

3️⃣ if...else Statement
Syntax
if (condition) {
  // true হলে
} else {
  // false হলে
}

উদাহরণ
let age = 15;

if (age >= 18) {
  console.log("Vote allowed");
} else {
  console.log("Vote not allowed");
}

4️⃣ if...else if...else (Multiple Condition)
উদাহরণ
let marks = 75;

if (marks >= 80) {
  console.log("A+");
} else if (marks >= 70) {
  console.log("A");
} else if (marks >= 60) {
  console.log("A-");
} else {
  console.log("Fail");
}

5️⃣ Nested if (if এর ভিতরে if)
let age = 25;
let hasNID = true;

if (age >= 18) {
  if (hasNID) {
    console.log("You can vote");
  } else {
    console.log("NID required");
  }
}

6️⃣ Comparison Operators

| Operator | Meaning              |
| -------- | -------------------- |
| ==       | equal (value)        |
| ===      | equal (value + type) |
| !=       | not equal            |
| !==      | not equal (strict)   |
| >        | greater              |
| <        | less                 |
| >=       | greater or equal     |
| <=       | less or equal        |


⚠️ Interview Tip
5 == "5"   // true
5 === "5"  // false


👉 Always use === in professional code

7️⃣ Logical Operators (AND, OR, NOT)
Operator	Meaning
&&	AND
	
!	NOT
উদাহরণ
let age = 20;
let nid = true;

if (age >= 18 && nid) {
  console.log("Vote allowed");
}

8️⃣ Ternary Operator (Short if else)
Syntax
condition ? truePart : falsePart

উদাহরণ
let age = 17;

let result = age >= 18 ? "Adult" : "Child";
console.log(result);

9️⃣ switch Statement
Syntax
switch (value) {
  case x:
    // code
    break;
  default:
    // code
}

উদাহরণ
let day = 3;

switch(day){
  case 1: console.log("Sunday"); break;
  case 2: console.log("Monday"); break;
  case 3: console.log("Tuesday"); break;
  default: console.log("Invalid day");
}

🔟 Truthy & Falsy (Interview Favourite)
Falsy values:
false
0
""
null
undefined
NaN

Truthy:

সবকিছু যা falsy না

if ("hello") { // true
}


1️⃣1️⃣ Short Circuit Evaluation
let name = "";

let result = name || "Guest"; // Guest

1️⃣2️⃣ Real Life Example
Login System
let email = "test@gmail.com";
let password = "1234";

if(email && password){
  console.log("Login Success");
}else{
  console.log("Login Failed");
}


🧪 Problem Solving Practice (with Solution)
🟢 Problem 1: Even / Odd
let num = 10;
console.log(num % 2 === 0 ? "Even" : "Odd");

🟢 Problem 2: Max of 3 numbers
let a=5, b=9, c=3;

if(a>b && a>c) console.log(a);
else if(b>c) console.log(b);
else console.log(c);

🟢 Problem 3: Grade Calculator
let marks = 85;

if(marks >= 80) console.log("A+");
else if(marks >= 70) console.log("A");
else if(marks >= 60) console.log("B");
else console.log("Fail");

🟢 Problem 4: Password Validation
let password = "123456";

if(password.length >= 6){
  console.log("Strong");
}else{
  console.log("Weak");
}

🟢 Problem 5: Role Based Access
let role = "admin";

if(role === "admin"){
  console.log("Full Access");
}else if(role === "user"){
  console.log("Limited Access");
}else{
  console.log("No Access");
}


🎯 Interview Q&A (Most Asked)
❓ Q1: if এবং switch পার্থক্য?

Answer:
if → complex condition
switch → fixed value compare

❓ Q2: == vs === ?

Answer:
== → type convert করে
=== → type convert করে না (recommended)

❓ Q3: Truthy/Falsy কী?

Falsy: false, 0, "", null, undefined, NaN
বাকি সব truthy

❓ Q4: Ternary operator কেন ব্যবহার করি?

Short condition লেখার জন্য

❓ Q5: Nested if ভালো practice?

না, বেশি হলে code messy হয়
→ logical operator ব্যবহার করো